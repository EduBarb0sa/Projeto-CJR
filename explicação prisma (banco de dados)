// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client { //isso aqui é formalidade do prisma
  provider = "prisma-client-js"
}

datasource db { //isso aqui define de onde vai pegar (sqlite) e q vai por no arquivo database.db que o proprio prisma cria
  provider = "sqlite"
  url      = "file:./database.db"
}

model User { //aqui criamos um user e damos atributos pra ele
  id  Int @id @default(autoincrement()) //esse id aumenta automaticamente a cada user novo e serve como identificador (@id)
  email String  @unique // esse @unique quer dizer que só pode ter um user pra cada e-mail
  senha String  
  nome  String
  genero  String
  cargo String  
}
